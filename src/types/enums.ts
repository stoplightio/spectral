export enum RuleType {
  VALIDATION = 'validation',
  STYLE = 'style',
}

export enum RuleFunction {
  ALPHABETICAL = 'alphabetical',
  LENGTH = 'length',
  PATTERN = 'pattern',
  SCHEMA = 'schema',
  TRUTHY = 'truthy',
  XOR = 'xor',
}
