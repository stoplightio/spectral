import { RulesetDefinition } from '@stoplight/spectral-core';

export default {
  extends: [],
  parserOptions: {
    incompatibleValues: 'off',
    duplicateKeys: 'warn',
  },
} as RulesetDefinition;
