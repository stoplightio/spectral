rules:
  info-matches-stoplight:
    summary: Info must contain Stoplight
    given: $.info
    type: style
    then:
      field: title
      function: pattern
      functionOptions:
        match: Stoplight
