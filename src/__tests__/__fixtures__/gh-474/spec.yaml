openapi: 3.0.0
info:
  title: Test
  version: 1.0.0
  contact:
    $ref: 'common.yaml#/info/contact'
paths:
  '/':
    get:
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: 'common.yaml#/components/schemas/ResponseType'
