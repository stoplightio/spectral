import { RawResult } from '../../types';
// import { ensureRule } from '../index';

export const rule1 = (object: object): RawResult[] => {
  const results: RawResult[] = [];

  console.log(object);

  return results;
};
