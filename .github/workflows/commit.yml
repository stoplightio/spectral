name: Commit
on:
  pull_request:
    branches: [develop]
jobs:
  test-node:
    runs-on: ubuntu-latest
    container:
      image: node:${{ matrix.node-version }}
    strategy:
      matrix:
        node-version:
        - '16.20'
        - '18.18'
        - '20.17'
    steps:
    - name: Checkout code
      uses: actions/checkout@v5.0.1
    - name: Cached dependencies
      uses: "./.github/actions/cached-dependencies"
    - name: Build
      run: yarn build
      shell: bash
    - name: Test node
      run: yarn pretest
      shell: bash
    - name: Run node tests
      run: yarn test.jest --ci --maxWorkers=3
      shell: bash
    - name: Test harness
      uses: "./.github/actions/test-harness"
      if: "'16.20' == ${{ matrix.node-version }} || '18.18' == ${{ matrix.node-version }}"
      with:
        os: linux
        node-version: "${{ matrix.node-version }}"
  test-windows:
    if: github.ref == 'refs/heads/develop'
    runs-on: windows-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v5.0.1
    - name: Cached dependencies
      uses: "./.github/actions/cached-dependencies"
      with:
        install-corepack: true
    - name: Build
      run: yarn build
      shell: powershell
    - name: Test node
      run: yarn pretest
      shell: powershell
    - name: Run node tests
      run: yarn test.jest --ci --maxWorkers=3
      shell: powershell

    - name: Test harness
      uses: "./.github/actions/test-harness"
      with:
        os: windows
        node-version: '18.18'
        max-workers: 3
  test-browser:
    runs-on: ubuntu-latest
    container:
      image: circleci/node:current-browsers
    steps:
    - name: Checkout code
      uses: actions/checkout@v4.1.0
    - name: Cached dependencies
      uses: "./.github/actions/cached-dependencies"
    - name: Build
      run: yarn build
      shell: bash
    - run: yarn pretest
    - name: Run browser tests
      run: yarn test.karma
  lint:
    runs-on: ubuntu-latest
    container:
      image: node:lts
    steps:
    - name: Checkout code
      uses: actions/checkout@v4.1.0
    - name: Cached dependencies
      uses: "./.github/actions/cached-dependencies"
      with:
        install-corepack: true
    - name: Build
      run: yarn build
      shell: bash
    - name: Lint commit message
      run: node ./scripts/lint-commit-messages.mjs
    - name: Lint code
      run: |-
        yarn prelint
        yarn lint.eslint
    - name: Lint documentation
      run: yarn lint.prettier
  release:
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    container:
      image: node:lts
    needs:
    - lint
    - test-node
    - test-windows
    - test-browser
    steps:
    - name: Checkout code
      uses: actions/checkout@v4.1.0
    - name: Prepare release
      uses: "./.github/actions/prepare-release"
    - name: Checkout code
      uses: actions/checkout@v4.1.0
    - name: Cached dependencies
      uses: "./.github/actions/cached-dependencies"
    - name: Set up NPM auth
      run: echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> .npmrc
    - name: Build
      run: yarn build
    - name: Release
      run: yarn release